# Runtime dependencies (mirrors setup.cfg's install_requires).
numpy
scipy
pymc
spiceypy

# Used throughout src/ and scripts/ but not yet declared in setup.cfg's
# install_requires -- listed here so `pip install -r requirements.txt` actually
# works for running the pipeline, not just importing photometry/photometry_etl.
pandas
pyarrow
planetaryimage
pvl
requests
matplotlib
duckdb
astropy  # lazy-imported by geometry_engine._DetachedFitsImage for .FIT image support

# Test/dev tooling.
pytest
hypothesis
ruff
